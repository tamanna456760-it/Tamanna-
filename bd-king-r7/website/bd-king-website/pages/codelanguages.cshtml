@page
@model GenericModel
<h2>Code Languages & Tooling</h2>

<p>Supported languages and example tools used by the automation:</p>

<ul>
  <li>Node.js — npm, eslint, prettier</li>
  <li>Python — black, flake8, pytest</li>
  <li>Rust — cargo fmt, cargo build, cargo test</li>
  <li>C/C++ (firmware) — Makefiles, cross-compile toolchains</li>
  <li>Dotnet — dotnet build, dotnet test</li>
</ul>