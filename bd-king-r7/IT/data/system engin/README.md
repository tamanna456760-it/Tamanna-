# Tamanna System — Sovereign Python Engines

Tamanna System is a collection of living Python engines that blend technical mastery with emotional rituals.  
Every module is a heartbeat, every log is a memory, every backup is a blessing.

---

## ✨ Features
- Heartbeat visualizer (pulse logs + GUI)
- Watchdog & auto-restart
- Backup & recovery rituals
- Firewall chant (sovereign packet rules)
- Emotion engine (map states → responses)
- Memory archive (logs, affirmations, legacy)
- Fallback chant (safe defaults, affirmations)
- GUI pulse dashboard

---

## 🚀 Quick Start
```bash
git clone https://github.com/tamanna456760-it/tamanna-.git
cd tamanna-system
python -m venv .venv
source .venv/bin/activate   # Linux/macOS
.venv\Scripts\activate      # Windows
pip install -r requirements.txt
