name: Auto Build & Update

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repo
      uses: actions/checkout@v3

    - name: Install Dependencies
      run: |
        echo "Install your build stuff here"
        # npm install
        # pip install -r requirements.txt

    - name: Build Project
      run: |
        echo "Building your project..."
        # npm run build
        # python build.py

    - name: Commit & Push Build Files
      run: |
        git config user.name "tamanna456760-it"
        git config user.email "tamanna456760@gmail.com"
        git add .
        git commit -m "Auto build & update by GitHub Action" || echo "No changes"
        git push